#include <stdio.h>
#include <conio.h> 

void main() {

    int x, y;
    printf("Enter the first array legnth: ");
    scanf("%d", &x);

    int a[x], b[2][x];

    printf("Enter the elements of first array: ");
    for(int i=0; i<x; i++) {
        scanf("%d", &a[i]);
        c[i]=a[i];
    }
}